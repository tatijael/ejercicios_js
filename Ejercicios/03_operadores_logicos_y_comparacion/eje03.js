Numero par o impar//

const number = prompt('Ingrese un valor');

const isOdd = number => {
	if (number % 2 == 0) {
		alert('Numero par');
	} else {
		alert('Numero impar');
	}
};

isOdd();

cajero //

let dineroDisponible = 5000;

const money = prompt(`Dinero disponible: ${dineroDisponible} retirar:`);
if (money - dineroDisponible);

money();
